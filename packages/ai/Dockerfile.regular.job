FROM python:3.12-slim

WORKDIR /app
COPY . .
RUN rm -rf build dist *.egg-info llm_demo/*.egg-info
RUN pip install --no-cache-dir -r requirements.txt

CMD ["python", "llm_demo/regular_job.py"]
